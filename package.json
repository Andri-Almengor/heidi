{
  "name": "heidi-quiz-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Heidi Quiz application with a React/Vite frontend and Node/Express backend connected to Google Apps Script and Sheets.",
  "main": "src/server.js",
  "type": "module",
  "engines": {
    "node": ">=22.12.0 <23.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "backend:dev": "node --watch src/server.js",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "check": "node --check src/server.js && node --check src/app.js && node --test && npm --prefix frontend run build",
    "test": "node --test",
    "render:build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "zod": "^3.25.76"
  }
}
